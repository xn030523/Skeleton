import type { SkillDef } from "../registry.js";

export const jsDeobfuscationSkill: SkillDef = {
  name: "js-deobfuscation",
  description: "JavaScript deobfuscation: reverse obfuscator.io, control-flow flattening, string array rotation, eval encoding, dead code injection. Use when analyzing obfuscated or minified JS in web targets.",
  category: "reverse-engineering",
  userInvocable: true,
  content: () => [
    "# JS Deobfuscation",
    "",
    "## First-Pass",
    "1. Open in browser DevTools Sources panel, hit {} to pretty-print",
    "2. Identify obfuscation type (see classification below)",
    "3. Set breakpoints at entry points, trace to the target function",
    "4. Hook eval/Function/atob — log before executing to capture decoded strings",
    "",
    "## Obfuscation Classification",
    "",
    "### Minified Only",
    "- Variable names collapsed, structure intact",
    "- Pretty-print restores readability, rename variables semantically",
    "",
    "### Eval/String Encoding",
    "- eval(atob(...)), eval(String.fromCharCode(...)), unescape(encodeURIComponent(...))",
    "- **Fix**: replace eval with console.log, run in console, capture output",
    "- For nested eval: wrap in a trap function that logs and returns the argument",
    "",
    "### Control Flow Flattening (CFF)",
    "- Big switch/dispatcher loop with state variable",
    "- Pattern: while(true) { switch(state) { case '0': ...; state='3'; break; ... } }",
    "- **Fix**: map each case, trace state transitions, rebuild linear control flow",
    "- Tool: deobfuscate-io, or manually annotate state transitions",
    "",
    "### String Array Rotation",
    "- _0x1234() accessor calls, string array at top of file, rotation offset applied on load",
    "- Pattern: var _0xabcd = ['log', 'Hello', ...]; function _0x1234(idx, offset) { ... }",
    "- **Fix**: extract array, compute decoded values with rotation applied, inline all calls",
    "- Use browser: set breakpoint in accessor, evaluate _0x1234(0x0) to get decoded string",
    "",
    "### Dead Code Injection",
    "- Huge if-else trees with always-false conditions",
    "- Pattern: if (0x1 === 0x2) { ... } or if (!_0xflag) { ... }",
    "- **Fix**: simplify with AST transform or manually prune branches",
    "",
    "## Deobfuscation Tools",
    "- **Browser DevTools**: Sources panel + breakpoints + console overrides (always available)",
    "- **jshook MCP**: hook function calls, trace args/returns in real-time",
    "- **deobfuscate-io**: web-based AST deobfuscator for obfuscator.io output",
    "- **js-deobfuscator**: Chrome extension that auto-deobfuscates in DevTools",
    "- **Babel plugins**: write custom AST transforms for project-specific patterns",
    "- **prettier**: format after deobfuscation for readability",
    "",
    "## Common Patterns Cheat Sheet",
    "- void 0x0 = undefined, !0x1 = true, ![] = false",
    "- Comma expressions: (a=1, b=2, c=3) evaluates left-to-right, returns last",
    "- _0xabcd = ['log', 'Hello', ...] + _0x1234() accessor = string array pattern",
    "- __proto__, constructor['fromCharCode'] = indirect property access evasion",
    "",
    "## Gotchas",
    "- String array rotation offset changes at load time — you MUST evaluate in the same runtime or compute offset manually",
    "- CFF state variables can be modified by side effects — trace actual execution, don't just read the switch cases",
    "- Anti-debug: setInterval checking debugger statement or timing — disable by overriding setInterval/console.log",
    "- Some obfuscators use Function() instead of eval — hook both",
    "- DOM-based obfuscation: script writes other scripts at runtime — watch DOM mutations",
    "",
    "## Workflow for obfuscator.io Output",
    "1. Pretty-print in browser",
    "2. Find string array declaration (usually top of file, big array literal)",
    "3. Find rotation function (shuffles array, called IIFE)",
    "4. Find accessor function (takes index, looks up in array)",
    "5. Evaluate accessor in browser console to decode all string references",
    "6. Replace accessor calls with decoded string literals",
    "7. Simplify CFF: trace state transitions, rebuild linear flow",
    "8. Remove dead code branches",
    "9. Rename variables semantically",
    "",
    "## When to Pivot",
    "- Target is WASM, not JS > wasm-reverse",
    "- Need to extract code from Webpack bundle > bundle-analysis",
    "- Analyzing Chrome extension code > chrome-extension-audit",
    "- Need to understand API calls in the JS > api-reverse",
  ].join("\n"),
};
