import type { SkillDef } from "../registry.js";

export const apiReverseSkill: SkillDef = {
  name: "api-reverse",
  description: "API reverse engineering: endpoint discovery, authentication bypass, request replay, GraphQL introspection, rate limit evasion. Use when reverse engineering web APIs or mobile app backends.",
  category: "reverse-engineering",
  userInvocable: true,
  content: () => [
    "# API Reverse Engineering",
    "",
    "## First-Pass",
    "1. Open Network tab, clear, perform complete user flow, save HAR",
    "2. Filter by XHR/Fetch — identify all API endpoints",
    "3. For each endpoint: method, URL pattern, headers, auth, request body, response",
    "4. Group endpoints by feature area (auth, data, admin, internal)",
    "",
    "## Endpoint Discovery",
    "",
    "### From Network Traffic",
    "- Filter by XHR/Fetch in DevTools",
    "- Sort by URL to see API base paths (/api/v1/, /graphql, /rest/)",
    "- Check Initiator column to find which JS code triggered the request",
    "",
    "### From JS Source",
    "- Search for: fetch(, axios., $.ajax, XMLHttpRequest, /api/, graphql",
    "- Look for API client classes with base URL configuration",
    "- Find route definitions in SPA frameworks: Vue Router, React Router paths",
    "- Environment variables or config files with API URLs",
    "",
    "### From Mobile App",
    "- Use mitmproxy-mcp or burp-mcp to intercept mobile app traffic",
    "- SSL pinning bypass: Frida hook on certificate validation (see frida-mcp)",
    "- APK/IPA analysis: jadx-mcp for APK, strings for API URLs",
    "",
    "## Authentication Patterns",
    "",
    "### Bearer Token / JWT",
    "- Decode at jwt.io — check alg, exp, sub, claims",
    "- Common weaknesses: alg:none, RS256→HS256 confusion, weak secret",
    "- flask-unsign or jwt-tool for brute-forcing JWT secrets",
    "- Check if tokens are validated server-side or just decoded",
    "",
    "### API Keys",
    "- Common headers: X-API-Key, Authorization: ApiKey ..., X-Auth-Token",
    "- Query params: ?api_key=..., ?token=...",
    "- Cookies: sessionid, auth_token, csrf_token",
    "- Check if key is tied to origin/referrer or usable from anywhere",
    "",
    "### OAuth / OIDC",
    "- Follow redirect_uri chain — check for open redirect",
    "- Capture authorization code + token exchange",
    "- Check state parameter (CSRF protection) — missing state = vulnerability",
    "- Scope escalation: request more scopes than granted",
    "",
    "## Request Replay & Modification",
    "",
    "### mitmproxy Workflow",
    "1. Enable mitmproxy-mcp to intercept traffic",
    "2. Capture a normal request",
    "3. Modify parameters and replay",
    "4. Test IDOR: change user_id in path or body",
    "5. Test privilege escalation: add admin fields",
    "",
    "### Manual Replay",
    "- Copy as cURL from DevTools (right-click request > Copy > Copy as cURL)",
    "- Modify and replay from terminal",
    "- Use api-tester-mcp for structured testing",
    "",
    "## GraphQL Specifics",
    "",
    "### Introspection",
    "- POST to /graphql with { __schema { types { name fields { name type { name } } } } }",
    "- If introspection enabled: full schema revealed",
    "- If disabled: try field suggestions (error messages may reveal field names)",
    "",
    "### Query Analysis",
    "- Look for __typename in queries — indicates fragment usage",
    "- Batch queries: [{ query: ... }, { query: ... }] — bypass rate limits",
    "- Depth attacks: nested queries can cause DoS",
    "",
    "## Rate Limit Bypass",
    "- X-Forwarded-For: rotate IP addresses",
    "- X-Real-IP, X-Originating-IP, X-Remote-IP headers",
    "- Different auth tokens per request",
    "- GraphQL batching: multiple queries in one request",
    "- Spread requests across multiple API keys",
    "",
    "## Gotchas",
    "- CORS errors ≠ security — server may still process the request; check with curl",
    "- Some APIs validate Content-Type strictly — always set correct headers on replay",
    "- CSRF tokens may be required even for API endpoints — capture fresh token per request",
    "- Pagination APIs may have undocumented limit parameters — try limit=9999 or limit=0",
    "- DELETE endpoints may accept body parameters for filtering — test non-obvious params",
    "- WebSocket APIs have different auth patterns — token in first message or in URL",
    "",
    "## When to Pivot",
    "- API client code is obfuscated > js-deobfuscation",
    "- Need to bypass anti-bot on API > anti-bot-bypass",
    "- Mobile app API with SSL pinning > use frida-mcp",
    "- Found WASM-based crypto in auth > wasm-reverse",
  ].join("\n"),
};
